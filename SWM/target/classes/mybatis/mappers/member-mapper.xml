<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">

	<insert id="signUpMember" parameterType="com.ez.swm.login.vo.SignUpForm" >
		INSERT INTO MEMBER VALUES(
			SEQ_MEM.NEXTVAl, #{userId}, #{userName}, #{nickname}, 
			#{password}, #{jumin1} || '-' || #{jumin2}, #{phone1} || '-' || #{phone2} || '-' || #{phone3} , DEFAULT
		) 
	</insert>
	
	<select id="loginMember" parameterType="com.ez.swm.login.vo.SignUpForm" resultType="com.ez.swm.login.vo.Member" >
		SELECT * 
		FROM MEMBER 
		WHERE USERID = #{userId} AND PASSWORD = #{password}
	</select>

</mapper>